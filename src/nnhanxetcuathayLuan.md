// Sử dụng AI để xây dự án thì:
// source thì oke 
// nhưng các sơ đồ thì sai quy tắc và ý nghĩa cách vẽ hết rồi, 
// chỉ đúng nếu dùng để coi như hiểu tổng quan dự án của mình thì được (chỉ đúng với minh)
// không thể sử dụng kiểu vẽ nội dung như vậy cho các dự án được (vì người khác đọc không hiểu)
// usecase thì vẽ sai ký hiệu actor, vẽ các tính năng chứ k phải kiểu tổng quan như mình vẽ
// sequence thì vẽ riêng cho 1 tính năng, còn mình chơi vẽ tổng quan toàn dự án
// uml thì phải vẽ tất cả các class, chứ k tách ra thành cách file vậy được, mặc dù tách ra thì chỉ có mình hiểu
// và cái uml thì vẽ đủ mối quan hệ thì chắc rối như mạng nhện



// Sửa lại như sau

// usecase diagram
// use case thì vẽ actor là hình người, và các chức năng là phân nhóm các chức năng (use case chung)
// Từ use case chung chọn 1 tính năng để vẽ usecase chi tiết

// sequence diagram
// thì vẽ luồng thực thi của 1 chức năng cụ thể thôi (vẽ đầy đủ các luồng)

// uml diagram
// vẽ tất cả class ra và có package luôn => mối quan hệ thì vẽ đặc trưng mấy cái quan trọng thôi còn không thì phải vẽ hết